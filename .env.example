# Public browser configuration. Copy to apps/web/.env.local for the Next.js app.
# Every NEXT_PUBLIC_ value is visible to visitors. Use only public configuration.
NEXT_PUBLIC_CHAIN_ID=421614
# Compatibility name: this must be SAMA demoUSDC, NOT Circle test USDC.
NEXT_PUBLIC_USDC_ADDRESS=replace_with_deployed_demo_usdc_address
NEXT_PUBLIC_PRIVY_APP_ID=replace_with_your_privy_app_id
# Optional local inspection overlay. Keep unset for ordinary demos and sensitive reveal material.
# NEXT_PUBLIC_ENABLE_REACT_DEVTOOLS=1
NEXT_PUBLIC_OFFERING_ADDRESS=replace_after_deployment
NEXT_PUBLIC_KIRA_TOKEN_ADDRESS=replace_after_deployment
NEXT_PUBLIC_MARKETPLACE_ADDRESS=replace_after_deployment
NEXT_PUBLIC_REGISTRY_ADDRESS=replace_after_deployment
NEXT_PUBLIC_DEMO_ACCESS_ADDRESS=replace_after_deployment
NEXT_PUBLIC_DEPLOYMENT_BLOCK=replace_after_deployment
NEXT_PUBLIC_DOCS_URL=https://sama-3.gitbook.io/sama-product-and-protocol/

# Server-only infrastructure. Never expose these with a NEXT_PUBLIC_ prefix.
ARBITRUM_SEPOLIA_RPC_URL=https://replace-with-an-authenticated-rpc.example
ARBISCAN_API_KEY=replace_with_your_arbiscan_api_key

# Deployment keys are intentionally absent. Use a Foundry encrypted keystore locally
# and GitHub Environment secrets in CI; never place a raw private key in an env file.
# The client integration does not need a Privy app secret.
